import {
    client_id as _client_id,
    client_secret as _client_secret
} from './keys';
export const opts = {
    apiEndpoint: "https://nameless-basin-66375.herokuapp.com",
    client_id: _client_id,
    client_secret: _client_secret
}